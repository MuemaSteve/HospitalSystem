  /* tabContainer.setRotateGraphic(true);
        EventHandler<Event> replaceBackgroundColorHandler = event -> {
            lastSelectedTabIndex = tabContainer.getSelectionModel().getSelectedIndex();

            Tab currentTab = (Tab) event.getTarget();
            if (currentTab.isSelected()) {
                currentTab.setStyle("-fx-background-color: -fx-focus-color;");
            } else {
                currentTab.setStyle("-fx-background-color: -fx-accent;");
            }
        };

        EventHandler<Event> logoutHandler = event -> {
            Tab currentTab = (Tab) event.getTarget();
            if (currentTab.isSelected()) {
                tabContainer.getSelectionModel().select(lastSelectedTabIndex);

logOut(panel);
System.out.println("Logging out!");
            }
        };

        configureTab(searchPatientTab, "FIND PATIENT", "resources/images/22-Cardi-B-Money.png", patientsearchcontainer, getClass().getResource("resources/views/Physicians/searchPatientInfo.fxml"), replaceBackgroundColorHandler);
        configureTab(addpatienthistoryTab, "ADD PATIENT HISTORY", "resources/images/22-Cardi-B-Money.png", addPatientHistoryContainer, getClass().getResource("resources/views/Physicians/addPatientHistory.fxml"), replaceBackgroundColorHandler);
        configureTab(labtestsTab, "LAB", "resources/images/22-Cardi-B-Money.png", labtestscontainer, getClass().getResource("resources/views/Physicians/labTests.fxml"), replaceBackgroundColorHandler);
        configureTab(accountTab, "MY ACCOUNT", "resources/images/22-Cardi-B-Money.png", accountContainer, getClass().getResource("resources/views/Physicians/account.fxml"), replaceBackgroundColorHandler);
        configureTab(logoutTab, "LOG OUT", "resources/images/22-Cardi-B-Money.png", null, null, logoutHandler);

        searchPatientTab.setStyle("-fx-background-color: -fx-focus-color;");
*/




/*
    private void configureTab(Tab tab, String title, String iconPath, AnchorPane containerPane, URL resourceURL, EventHandler<Event> onSelectionChangedEvent) {
        double imageWidth = 40.0;

        ImageView imageView = new ImageView(new Image(iconPath));
        imageView.setFitHeight(imageWidth);
        imageView.setFitWidth(imageWidth);

        Label label = new Label(title);
        label.setMaxWidth(tabWidth - 20);
        label.setPadding(new Insets(5, 0, 0, 0));
        label.setStyle("-fx-text-fill: black; -fx-font-size: 8pt; -fx-font-weight: normal;");
        label.setTextAlignment(TextAlignment.CENTER);

        BorderPane tabPane = new BorderPane();
//        tabPane.setRotate(90.0);

        tabPane.setMaxWidth(tabWidth);
        tabPane.setCenter(imageView);
        tabPane.setBottom(label);

        tab.setText("");
        tab.setGraphic(tabPane);

        tab.setOnSelectionChanged(onSelectionChangedEvent);

        if (containerPane != null && resourceURL != null) {
            try {
                Parent contentView = FXMLLoader.load(resourceURL);
                containerPane.getChildren().add(contentView);
                AnchorPane.setTopAnchor(contentView, 0.0);
                AnchorPane.setBottomAnchor(contentView, 0.0);
                AnchorPane.setRightAnchor(contentView, 0.0);
//                AnchorPane.setLeftAnchor(contentView, 0.0);
            } catch (IOException e) {
                e.printStackTrace();
            }
        }
    }
*/
